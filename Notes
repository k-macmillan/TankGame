NOTES:
Moving into a tree causes a collision (dmg)
Moving into the wall causes a collision (dmg)
You can move diagonal through trees (no dmg)
You can shoot projectiles (no dmg)
    Uknown if harder hitting bullets continue through weaker bullets
Projectiles travel 6 block per round (not action, round)


TODO:
enemy AP estimator based on movement + shots
Shot trajectory (to avoid dying)
